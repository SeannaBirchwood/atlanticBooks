<div>

<div id="wrapper">
  <div id="columns">
    <div>
    <h1>Featured Author:</h1>
    <h3>George Elliott Clarke</h3>
  <% @feat_auth.each do |book| %>
    <article class="pin">
      <img src="<%= book.image_link(:zoom => 5) %>" />
        <a href="<%= book.info_link %>" target="_blank"><h4 class="title"><%= book.title %></h4></a>
         <p>by <%= book.authors %></p>
        <p class="cat"><%= book.publisher %>
        <br>
        <%= book.categories %></p>
        <br>
        <%= link_to "Add to Favorites" %>
        <br>
        <%= link_to "Add to Wishlist" %>
      </article>
     <% end %>
    </div>
  </div>
</div>
</div>