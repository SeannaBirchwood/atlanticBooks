<div>

<div id="wrapper">
  <div id="columns">
    <div>
    <h1>Featured Publisher:</h1>
    <h2>Goose Lane Editions</h2>
  <% @feat_pub.each do |book| %>
    <article class="pin">
      <img src="<%= book.image_link(:zoom => 5) %>" />
        <a href="<%= book.info_link %>" target="_blank"><h4 class="title"><%= book.title %></h4></a>
         <p>by <%= book.authors %></p>
        <p class="cat"><%= book.publisher %>
        <br>
        <%= book.categories %></p>
        <br>
        <%= link_to "Add to Favorites" %>
        <br>
        <%= link_to "Add to Wishlist" %>
      </article>
     <% end %>
    </div>
  </div>
</div>
</div>