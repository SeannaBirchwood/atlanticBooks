<article class="user">
  <header>
    <%= link_to 'Show', user %>
      <%= image_tag user.avatar if user.avatar.present? %>
      <h4>Username: <%= user.username %></h4>
  </header>
  <p class="location">
    City: <%= user.location %>
  </p>
</article>
